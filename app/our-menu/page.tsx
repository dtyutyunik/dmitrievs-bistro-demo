import { menuSections } from "../site-data";
import { SiteFooter, SiteHeader } from "../site-shell";
export default function OurMenu() { return <main><SiteHeader active="menu"/><section className="menu-page"><h1>OUR MENU / НАШЕ МЕНЮ</h1><nav className="menu-jump" aria-label="Menu categories">{menuSections.map(section=><a key={section.id} href={`#${section.id}`}>{section.title.split(" / ")[0]}</a>)}</nav>{menuSections.map(section=><section className={`official-menu-section ${section.id === "lunch" ? "lunch-section" : ""}`} id={section.id} key={section.id}><h2>{section.title}</h2><div className="dish-grid">{section.dishes.map(dish=><article className={`dish-card ${!dish.image ? "text-dish" : ""}`} key={dish.name}>{dish.image && <div className="dish-image"><img src={dish.image} alt={dish.name}/></div>}<h3>{dish.name} <span>— {dish.price}</span></h3>{dish.description && <p>{dish.description}</p>}</article>)}</div></section>)}<p className="price-note">Prices are subject to change. Please confirm current pricing when ordering.</p></section><SiteFooter/></main>; }

